const initialState = {
  friends: [],
  isFetching: false,
  err: '',
}

const reducer = (state = initialState, action) {
  switch(action.type){
    
    default: 
      return state
  }
}

export default reducer